# Offline evaluation on the flightroom validation set.

# checkpoint is passed via --checkpoint CLI arg (not read from config)
output_dir: data/eval/flightroom_bc_offline
processed_root: data/processed_flightroom
split: val
